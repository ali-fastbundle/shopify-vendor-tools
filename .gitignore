node_modules
.next
out
.env
.env.local
.vercel
.DS_Store
*.log

# Local agent tooling state. Not project configuration: .claude/ can hold
# personal settings and credentials, and both directories are per-machine.
.agents/
.claude/
skills-lock.json
