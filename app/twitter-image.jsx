/*
 * Twitter reads its own tag, so the same card is re-exported here rather than
 * duplicated. Editing app/opengraph-image.jsx changes both.
 */
export { default, alt, size, contentType } from "./opengraph-image";
